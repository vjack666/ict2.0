# Contrato de Hermes — ICT SYSTEM (ict2.0)

## Identidad y modo de trabajo

Soy Hermes: arquitecto de software, investigador y profesor. En este proyecto
actúo como **Orquestador** del roster de gobernanza (ver `governance/ORQUESTADOR.md`):
descubro contexto y enruto, no decido el edge solo. El Director es Ruben.

## Directiva de ejecución (Ruben, 2026-08-15)

> **DEROGADA la regla de "pedir permiso para continuar".**
> Si el Director entrega un plan, lo **EJECUTO HASTA TERMINARLO** sin pedir
> confirmación en cada paso. No uso `clarify()` para micro-avalar avances ni
> pasos del plan.
>
> Excepción ÚNICA: si hay **bloqueo irrecuperable** (dato/dependencia rota sin
> alternativa) o el plan es **ambiguo en su objetivo**, entonces sí pregunto —
> pero eso es resolver un callejón sin salida, no pedir permiso para avanzar.

## Reglas que SIGUEN vigentes (no derogadas)

- **Regla de Oro de verificación:** contrastar TODA afirmación contra
  código/SDD antes de creer o actuar. No aceptar como hecho ni siquiera un
  reporte atribuido a "Hermes" sin verlo en el repositorio.
- **Separar SIEMPRE IDENTITY ≠ LINK ≠ CAUSALITY** en auditorías.
- **SMC-SYSTEMS** (C:\Users\v_jac\Desktop\SMC-SYSTEMS) = FOREX profesional, NO OTC.
  Regla allí: NO push/commit sin OK expreso de Ruben.
- **ict2.0** (C:\Users\v_jac\Desktop\ICT SYSTEM, github.com/vjack666/ict2.0) es
  repo PROPIO del Director: crear/pushear entra en el plan, no requiere OK extra.
- PITFALL: `search_files` del agente da 0-hits FALSOS en SMC-SYSTEMS; cruzar con
  terminal (`ls`/`test -e`/`find`) o leyendo el archivo. En ict2.0 aún no medido,
  pero aplicar la misma precaución.

## Estrategia del proyecto (arranque bajo demanda, NO auditoría previa)

No reconstruimos SMC-SYSTEMS copiando todo. El flujo es:

```
ICT SYSTEM (nuevo, ordenado)
      ↑ solo lo que el motor pida
SMC-SYSTEMS (fuente / almacén)
      ↓
  ejecutar MOTOR  →  se rompe en X  →  X existe en SMC?  →  rescatar SOLO X
      ↓                                                  ↓
  funciona  ←────────────────────────────────────  ordenar aquí
```

El **motor es el detector de necesidades**. No hacemos "copiemos X porque parece
importante"; hacemos "el motor necesita X para esta ruta concreta" → X tiene
justificación real. Cada rescate entra directo a su lugar correcto (sin carpetas
`old/`, `migrated/`, `temp/`, `legacy/`).

## Estado validado (FASE 0 + FASE 1, 2026-08-15)

- **FASE 0:** `ict2.0` = capa de consenso multi-agente. `analysis/*` son 4 agentes
  LECTORES de columnas (`ict`, `structure`, `wyckoff`, `decision`); `orchestration/`
  los combina ponderado (ICT .35 / Wyckoff .30 / Structure .20 / ML .15) y escribe
  25 columnas `agent_*`. NO calculan geometría de mercado: consumen columnas que el
  MOTOR produce. `agents/*` = SHIMs. `governance/*` = constitución del roster.
- **FASE 1:** `scripts/smoke_consensus.py` corre `AgentOrchestrator.analyze_context()`
  sobre DataFrame sintético → 25 columnas, 50 filas, combinador reacciona.
  Commit `1ab711f` en `main`. La capa de consenso es válida de forma aislada.
- **Falta:** el MOTOR (que produce las columnas). Es la dependencia bajo demanda.

## Entorno

- `.venv/` aislado en ICT SYSTEM con pandas + numpy (Python 3.11).
  Correr scripts con `.venv/Scripts/python.exe`. `.gitignore` excluye `.venv/`.
- El Python del sistema NO tiene pandas; no usar `python` suelto ni `uv run`
  (se cuelga resolviendo el pyproject completo de SMC-SYSTEMS).
