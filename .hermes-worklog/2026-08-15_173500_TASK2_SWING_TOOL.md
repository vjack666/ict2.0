# Bitácora — Task 2: tools/swing.py (primera herramienta individual REAL)

**Fecha:** 2026-08-15 17:35 UTC-5
**Plan:** `.hermes/plans/2026-08-15_143000-individual-tools-m5-learning.md` (Fase 1, Task 2)
**Veredicto aplicado:** aislamiento arquitectónico — NO absorber legado de engine/.

---

## Objetivo
Primera herramienta individual real: SWING (alto/bajo → HH/LH/HL/LL), base de la plantilla vela-a-vela.

## Qué importó REALMENTE (el detalle que el Director pidió mirar)
`tools/swing.py` importa SOLO:
- `pandas`, `numpy` (stdlib de datos)
- `tools.base.SingleTool`, `tools.event.ToolEvent` (la frontera del Task 1)

NO importa `detectors.bos`, NO importa nada de `engine/`.

**Decisión de aislamiento:** en lugar de `from detectors.bos import _swing_points, _label_swings`, REENVUELVO ambas funciones dentro de `tools/swing.py` como algoritmos puros (solo pandas/numpy, sin ffill que contamine). Así `SwingTool` no crea dependencia lateral hacia `detectors.bos.py` (que aunque hoy es puro, mañana podría tirar de ATR/sweeps/engine).

Verificación de aislamiento (runtime): tras `import tools.swing`, `sys.modules` NO contiene ningún módulo `engine.*` ni `detectors*`. 🟢

## Verificación funcional (EURUSD M5, 1 mes recortado 2026-07-14→08-14)
- Filas M5: 6540.
- ToolEvents SWING emitidos: **840** (pivots reales, no todas las velas).
- Distribución: HH=195, LH=223, HL=222, LL=200 (equilibrada).
- Log append-only: `data/learning/swing/EURUSD_M5_2026-07.jsonl` → 840 líneas, `human_score: null`.

## Bugs corregidos durante el test
1. `pd.NA in (...)` → TypeError (NA booleano ambiguo). Cambiado a `pd.isna(lab)`.
2. `_swing_points` hacía `.ffill()` → casi todas las velas parecían pivot. Eliminado ffill: solo la vela pivot lleva valor; `_to_events` emite solo donde `swing_high`/`swing_low` no son NA.

## Hallazgo de datos
- `data/raw/EURUSD/EURUSD_M5.parquet` tiene 334,852 velas (histórico 2022→2026), NO solo 1 mes. El caller debe recortar la ventana de 1 mes (hecho en el test; el contrato `run(df,...)` recibe el df ya recortado).
- El log se etiqueta por mes de INICIO del recorte (`2026-07`), aunque cubre jul-ago. No es bug; es convención. Ajustable si el Director prefiere mes de fin.

## Siguiente
Task 3: `tools/bos.py` (envuelve `detectors.bos.detect_bos` de forma aislada, emitiendo BOS_UP/BOS_DOWN con nivel). Mismo criterio de aislamiento.
