# Bitácora 2026-08-20 (parte 2) — Parche motor O(n) + TNA 20Y paralelo

**Fecha:** 2026-08-20 09:30 UTC-5
**Responsable:** Hermes (ejecutor)
**Contexto:** Retomar parche de `engine/mtf_navigation.py` para TNA 20Y local usando 20 cores.

---

## [DIAGNÓSTICO BASELINE]

Medido con evidencia (scripts/diag_nav_baseline_csv.py, CSV dukascopy 20Y):
- init run_sequential: 12.6s
- navigate: **667.6 ms/bar** (O(n²): _snapshot recalcula swings+BOS+displacement por barra)
- status OK, layers D1/H4/H1, D1 BULLISH / H1 BEARISH

**BUG de datos hallado:** mis parquet `data/raw/EURUSD/EURUSD_D1.parquet` y `H4.parquet`
están truncados a 2020+ (D1 arranca 2020-01-02, H4 similar) mientras H1/M1/M5 tienen 2006+.
El motor original navega bien; el "INCOMPLETE" del baseline parquet fue por datos faltantes.
Grok usó CSV dukascopy (2006+) y el funnel salió ok_rate 1.0. **Deuda D4: usar CSV para TNA.**

## [PARCHE v3 — O(n) precompute]

Reescritura de `_snapshot` + nuevo `_precompute_layers` en `__init__`:
- Precompute por capa UNA vez (O(n) por capa, SIN loop por barra — esto fue el bug de v2):
  - swings (causal), BOS por barra (detect_bos ya vectorizado causal), last_bos prefijo (ffill O(n)),
    displacement reciente (rolling O(n)), dealing range (rolling O(n)).
- En navigate: lookup O(1)/O(log n): bisect sobre swings ≤ i, bias en swing bars,
  last_bos[i] O(1), disp_recent[i] O(1), eq_zones via `_eq_pools(sh_u, ..., i)` EXACTO al original.

**Crítico:** v2 se colgó por loop por barra en precompute (slice `sh[:i+1]` por barra). v3
precompute TODO vectorizado una vez. eq_zones se calculan en _snapshot igual que el original
(no precompute global + filtro, porque eso cambiaba la agrupación de _eq_pools).

## [VALIDACIÓN]

1. Velocidad (diag_nav_baseline_csv.py con parche): init 16.9s, navigate **18.9 ms/bar** → 35× más rápido.
2. Regresión bit-exact (regression_nav_strict.py, 200 navigates × 3 capas vs motor original
   desde origin/main): **0 diffs / 600 layer-checks → BIT-EXACT**.
3. Navegación: status OK, mismos biases que baseline.

## [TNA 20Y — EN CURSO]

Script scripts/tna_20y_parallel.py: multiprocessing.Pool(20), navega TODAS las barras H1
(124.377) repartidas en 20 chunks. Cada worker crea su navigator (init ~17s en paralelo) y
navega su lote. Emite TNA-TRACE-INTEGRITY (asof_bar ≤ decision index en cada capa) y
TNA-BEHAVIORAL (status dist, bias dist, zonas, path len).

Estimado: 124377 × 18.9ms / 20 cores ≈ 2 min efectivo + init paralelo.

## [HALLAZGOS]

- El motor original SÍ era O(n²) (confirmado 667ms/bar), no bug de lógica.
- Parche v3 es bit-exact y 35× más rápido → TNA 20Y local viable en minutos.
- Deuda D4: parquet D1/H4 truncados; TNA usa CSV dukascopy (2006+).
- RAM: 20 workers × ~800MB ≈ 16GB límite — borderline pero OK para 124k barras.

## [SIGUIENTE]

- Si TNA pass ambos gates → commit parche + scripts + reporte, push.
- Luego SEQUENCE×CONTEXT STATE re-correr con más n (Grok).
